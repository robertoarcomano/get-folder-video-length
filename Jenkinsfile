// Jenkinsfile for building and testing Masterit
pipeline {
    agent any
    // Let's test
    stages {
        stage('show directory contents') {
            steps {
                script {
                    echo "Develop new proposal"
                    sh("ls -al")
                }
            }
        }
    }
}
